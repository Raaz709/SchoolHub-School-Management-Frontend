# SchoolHub Frontend Environment Variables Example
# Copy this file to .env.local or .env and configure your values

# Backend API origin (no trailing slash, no /api suffix)
# Controller routes already include the "api/" prefix.
VITE_API_BASE_URL=http://localhost:5000
